% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/risk_score.R
\name{risk_score}
\alias{risk_score}
\alias{print.risk_report}
\alias{as.data.frame.risk_report}
\alias{[.risk_report}
\title{Score function calls for reproducibility risk}
\usage{
risk_score(
  audit,
  methods = c("changelog", "seed_check", "locale_check"),
  min_risk = "low",
  major_version_grace = 1L
)

\method{print}{risk_report}(x, ...)

\method{as.data.frame}{risk_report}(x, ...)

\method{[}{risk_report}(x, i, j, ...)
}
\arguments{
\item{audit}{An \code{audit_report} object returned by \code{\link[=audit_script]{audit_script()}}.}

\item{methods}{\code{character}. Which checks to run. Any combination of
\code{"changelog"}, \code{"seed_check"}, \code{"locale_check"}. Default: all three.}

\item{min_risk}{\code{character(1)}. Minimum risk level to include in the output.
One of \code{"low"} (show all), \code{"medium"}, or \code{"high"}. Default \code{"low"}.}

\item{major_version_grace}{\code{integer(1)} or \code{Inf}. Number of full major
versions the installed package must be \emph{ahead} of \code{from_version} before
the entry is suppressed entirely. When the installed version is this many
or more major versions newer than \code{from_version}, the user is already past
the breaking-change transition and the flag is a false positive -- the
entry is silently dropped from the results. Set to \code{Inf} to disable.
Default \code{1L}.}

\item{x}{A \code{risk_report} object (for \code{print}, \code{as.data.frame}, and \code{[}).}

\item{...}{Additional arguments (currently unused).}

\item{i}{Row index.}

\item{j}{Column index. When columns are subsetted and required columns are
removed, the \code{"risk_report"} class is stripped so that
\code{print.risk_report()} is not called on an incomplete object.}
}
\value{
A \code{data.frame} of class \code{c("risk_report", "data.frame")} with one
row per flagged call. Columns:
\describe{
\item{\code{file}}{Source file path.}
\item{\code{line}}{Line number of the call.}
\item{\code{call}}{The \code{pkg::fn} string.}
\item{\code{pkg_version}}{Installed or lockfile-resolved version.}
\item{\code{risk}}{\code{"high"}, \code{"medium"}, or \code{"low"}.}
\item{\code{check}}{Which check flagged it: \code{"changelog"}, \code{"seed_check"},
or \code{"locale_check"}.}
\item{\code{description}}{Plain-English explanation of the risk.}
\item{\code{reference}}{URL to the relevant changelog or documentation.}
}
Rows are ordered by risk severity (high first), then by file and line.
If no risks are found, an empty data frame with the same columns is
returned.
}
\description{
Takes an \code{audit_report} and checks every detected \code{pkg::fn} call against
three independent checks:
\itemize{
\item \strong{\code{"changelog"}} -- matches against a curated database of known breaking
changes in popular CRAN packages, flagging calls where the installed
version falls in a known-risky version window.
\item \strong{\code{"seed_check"}} -- flags stochastic functions (\code{rnorm}, \code{sample}, etc.)
where no \code{set.seed()} appears within 50 lines above the call.
\item \strong{\code{"locale_check"}} -- flags functions whose output is locale-sensitive
(\code{sort()}, \code{format()}, \code{tolower()}, etc.).
}
}
\section{Version windows}{

The \code{"changelog"} check uses a half-open version window \verb{(from_ver, to_ver]}:
a call is flagged only if the installed version is \emph{greater than}
\code{from_ver} \emph{and} \emph{at most} \code{to_ver}. This means the risk is scoped to
versions where the breaking change is known to apply.
}

\section{Major version grace}{

When an installed version is \code{major_version_grace} or more major versions
ahead of \code{from_version}, the entry is suppressed entirely. The user is
already past the breaking-change transition -- flagging it at any severity
would be a false positive. The database staleness check
(\code{\link[=check_db_staleness]{check_db_staleness()}}) handles the maintenance concern of
identifying entries whose \code{from_version} floor is too old.
}

\examples{
script <- tempfile(fileext = ".R")
writeLines(c(
  "x <- dplyr::summarise(mtcars, n = dplyr::n())",
  "y <- stats::rnorm(100)",
  "z <- base::sort(letters)"
), script)

report <- audit_script(script, renv = FALSE, verbose = FALSE)
risks <- risk_score(report)
print(risks)

# High-severity items only
risk_score(report, min_risk = "high")

# Only the changelog check
risk_score(report, methods = "changelog")

}
\seealso{
\code{\link[=audit_script]{audit_script()}} to generate the input;
\code{\link[=repro_report]{repro_report()}} to render the results;
\code{\link[=check_db_staleness]{check_db_staleness()}} to identify database entries with
windows that are too wide.
}
