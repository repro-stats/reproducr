% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/certify.R
\name{check_drift}
\alias{check_drift}
\title{Check analytical outputs for drift against a certified baseline}
\usage{
check_drift(
  outputs,
  against = "latest",
  file = ".reproducr",
  tolerance = 1e-10
)
}
\arguments{
\item{outputs}{A fully named list of current R objects — the same names used
in the \code{\link[=certify]{certify()}} call being compared against.}

\item{against}{\code{character(1)}. The certification tag to compare against.
Use \code{"latest"} (the default) to automatically select the most recently
added certification.}

\item{file}{\code{character(1)}. Base path of the certification store.
Default \code{".reproducr"} (reads \code{.reproducr.rds}).}

\item{tolerance}{\code{numeric(1)}. Numeric tolerance applied to hash comparison.
When \verb{> 0}, outputs whose hashes differ are also compared element-wise
(for numeric vectors/matrices), and flagged as \code{"ok"} if the maximum
absolute difference is within \code{tolerance}. Set to \code{0} for exact matching
only. Default \code{1e-10}.}
}
\value{
Invisibly returns a \code{data.frame} of class
\code{c("drift_report", "data.frame")} with columns \code{output}, \code{status}
(\code{"ok"}, \code{"drifted"}, \code{"missing"}, \code{"new"}), \code{max_delta}, and \code{note}.
Also prints a summary to the console.
}
\description{
Re-hashes a set of named R objects and compares them against a previously
stored certification. Reports which outputs are unchanged (\code{"ok"}), have
changed (\code{"drifted"}), are present in the baseline but not supplied
(\code{"missing"}), or are new outputs not in the baseline (\code{"new"}).
}
\examples{
cert_file <- tempfile()
model <- lm(mpg ~ wt, data = mtcars)

certify(list(coefs = coef(model)), tag = "v1", file = cert_file)

# Same outputs — should report "ok"
result <- check_drift(list(coefs = coef(model)),
                       against = "v1", file = cert_file)
print(result)

# Different model — should report "drifted"
model2 <- lm(mpg ~ hp, data = mtcars)
check_drift(list(coefs = coef(model2)),
             against = "v1", file = cert_file)

}
\seealso{
\code{\link[=certify]{certify()}} to create a baseline; \code{\link[=list_certs]{list_certs()}} to see available
tags.
}
