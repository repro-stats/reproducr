% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/certify.R
\name{certify}
\alias{certify}
\title{Certify analytical outputs as a reproducibility baseline}
\usage{
certify(outputs, tag, script = NULL, file = ".reproducr")
}
\arguments{
\item{outputs}{A fully named list of R objects to certify. Each element is
hashed using SHA-256 (or a base-R fallback if \code{digest} is not available).
Common choices: \code{coef(model)}, \code{summary(model)$r.squared}, a results
\code{data.frame}, or any key scalar.}

\item{tag}{\code{character(1)}. A human-readable label for this certification,
e.g. \code{"submission-v1"} or \code{"pre-review"}. Tags must be unique within a
certification file; passing a duplicate tag overwrites the existing record
with a warning.}

\item{script}{\code{character(1)} or \code{NULL}. Path to the script that produced
these outputs. Used for documentation in the certification record only;
not validated. Default \code{NULL}.}

\item{file}{\code{character(1)}. Base path for the certification store. The
actual file written is \code{paste0(file, ".rds")}. Default \code{".reproducr"},
which writes \code{.reproducr.rds} in the current working directory.
Commit this file to version control.}
}
\value{
Invisibly returns the certification record (a list). Prints a
one-line summary to the console.
}
\description{
Hashes a named list of R objects (model coefficients, summary statistics,
key scalars, data frames) and saves them alongside full environment metadata
to a local certification file (\code{.reproducr.rds} by default). Later runs
can call \code{\link[=check_drift]{check_drift()}} to verify that results have not changed.

Think of \code{certify()} as a "signed receipt" for a completed analysis run.
}
\section{Certification store}{

All certifications for a project are accumulated in a single \code{.reproducr.rds}
file. You can have multiple tags representing different stages (e.g. before
and after peer review). Use \code{\link[=list_certs]{list_certs()}} to inspect stored tags.
}

\section{Version control}{

Commit \code{.reproducr.rds} to your project's version control repository.
This makes the certification auditable and shareable with collaborators.
}

\examples{
model <- lm(mpg ~ wt, data = mtcars)

cert_file <- tempfile()

certify(
  outputs = list(
    coefs     = coef(model),
    r_squared = summary(model)$r.squared,
    n_obs     = nrow(mtcars)
  ),
  tag = "baseline-v1",
  script = "analysis.R",
  file = cert_file
)

# See what is stored
list_certs(file = cert_file)

}
\seealso{
\code{\link[=check_drift]{check_drift()}} to compare current outputs against a baseline;
\code{\link[=list_certs]{list_certs()}} to inspect stored certifications.
}
