% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/certify.R
\name{list_certs}
\alias{list_certs}
\title{List all certifications stored in a certification file}
\usage{
list_certs(file = ".reproducr")
}
\arguments{
\item{file}{\code{character(1)}. Base path of the certification store.
Default \code{".reproducr"}.}
}
\value{
A \code{data.frame} with columns \code{tag}, \code{timestamp}, \code{r_version},
\code{os}, \code{n_outputs}, \code{script} — one row per certification.
Returns an empty data frame if no certifications exist.
}
\description{
A convenience function to inspect what certification tags are stored and
their key metadata, without needing to read the raw \code{.rds} file.
}
\examples{
cert_file <- tempfile()
model <- lm(mpg ~ wt, data = mtcars)

certify(list(coefs = coef(model)), tag = "v1", file = cert_file)
certify(list(coefs = coef(model)), tag = "v2", file = cert_file)

list_certs(file = cert_file)

}
