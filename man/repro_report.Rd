% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/repro_report.R
\name{repro_report}
\alias{repro_report}
\title{Generate a human-readable reproducibility report}
\usage{
repro_report(
  audit,
  risks = NULL,
  drift = NULL,
  format = "text",
  style = "minimal",
  output_file = NULL
)
}
\arguments{
\item{audit}{An \code{audit_report} object from \code{\link[=audit_script]{audit_script()}}. Required.}

\item{risks}{A \code{risk_report} data frame from \code{\link[=risk_score]{risk_score()}}. Optional but
strongly recommended — without it, the report cannot assess reproducibility.}

\item{drift}{A \code{drift_report} data frame from \code{\link[=check_drift]{check_drift()}}. Optional.}

\item{format}{\code{character(1)}. Output format: \code{"text"} (console),
\code{"md"} (Markdown file), or \code{"html"} (HTML file). Default \code{"text"}.}

\item{style}{\code{character(1)}. Report style: \code{"minimal"}, \code{"academic"}, or
\code{"pharma"}. Default \code{"minimal"}.}

\item{output_file}{\code{character(1)} or \code{NULL}. Output file path (used for
\code{format = "md"} and \code{format = "html"}). If \code{NULL}, a sensible default
name is used (\code{"reproducr_report.md"} / \code{"reproducr_report.html"}).}
}
\value{
Invisibly returns the report content as a character string. For
file-based formats, the file is also written to disk.
}
\description{
Renders a reproducibility audit report from an \code{\link[=audit_script]{audit_script()}} result
and optionally a \code{\link[=risk_score]{risk_score()}} result and \code{\link[=check_drift]{check_drift()}} result. Three
style presets are available:
\itemize{
\item \strong{\code{"minimal"}} — compact summary suitable for console review or internal
project documentation.
\item \strong{\code{"academic"}} — generates a ready-to-paste methods paragraph for journal
submissions, listing all packages with versions and summarising risk findings.
\item \strong{\code{"pharma"}} — structured QC document with a risk register and sign-off
fields, suitable for pharmaceutical or regulated analytical workflows.
}
}
\examples{
script <- tempfile(fileext = ".R")
writeLines(c(
  "set.seed(42)",
  "x <- dplyr::filter(mtcars, cyl == 4)",
  "y <- stats::rnorm(10)"
), script)

report <- audit_script(script, renv = FALSE, verbose = FALSE)
risks  <- risk_score(report)

# Console summary
repro_report(report, risks, format = "text", style = "minimal")

# Academic methods paragraph (printed, not written to file)
cat(repro_report(report, risks, format = "text", style = "academic"))

}
\seealso{
\code{\link[=audit_script]{audit_script()}}, \code{\link[=risk_score]{risk_score()}}, \code{\link[=check_drift]{check_drift()}}, \code{\link[=repro_badge]{repro_badge()}}
}
