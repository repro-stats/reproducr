% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/repro_badge.R
\name{repro_badge}
\alias{repro_badge}
\title{Generate a reproducibility status badge}
\usage{
repro_badge(
  audit,
  risks = NULL,
  drift = NULL,
  output = "markdown",
  readme_path = "README.md"
)
}
\arguments{
\item{audit}{An \code{audit_report} from \code{\link[=audit_script]{audit_script()}}.}

\item{risks}{A \code{risk_report} from \code{\link[=risk_score]{risk_score()}}. Optional.}

\item{drift}{A \code{drift_report} from \code{\link[=check_drift]{check_drift()}}. Optional.}

\item{output}{\code{character(1)}. \code{"markdown"} (return the badge string) or
\code{"README"} (insert/update the badge in \code{README.md}). Default \code{"markdown"}.}

\item{readme_path}{\code{character(1)}. Path to the README file when
\code{output = "README"}. Default \code{"README.md"}.}
}
\value{
Invisibly returns the badge Markdown string.
}
\description{
Produces a \href{https://shields.io}{shields.io} Markdown badge reflecting the
current reproducibility status of a project. The badge is colour-coded:
\itemize{
\item \strong{Green} (\code{reproducible}) -- no risks detected.
\item \strong{Yellow} (\code{caution}) -- medium-severity risks only.
\item \strong{Red} (\verb{at risk}) -- one or more high-severity risks or drifted outputs.
\item \strong{Grey} (\code{unknown}) -- no risk information supplied.
}

Can be inserted automatically into a \code{README.md} (e.g. from a GitHub
Actions workflow).
}
\examples{
script <- tempfile(fileext = ".R")
writeLines("x <- dplyr::filter(mtcars, cyl == 4)", script)
report <- audit_script(script, renv = FALSE, verbose = FALSE)
risks <- risk_score(report)

badge <- repro_badge(report, risks)
cat(badge)

}
\seealso{
\code{\link[=repro_report]{repro_report()}}, \code{\link[=risk_score]{risk_score()}},
\code{\link[=check_drift]{check_drift()}}
}
